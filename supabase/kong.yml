# Kong declarative config for local Supabase gateway.
# Routes: /rest/* -> postgrest
_format_version: "3.0"

services:
  - name: rest-service
    url: http://rest:3000
    routes:
      - name: rest
        paths:
          - /rest/v1
        strip_path: true
